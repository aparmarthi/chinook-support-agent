# Python 3.12 or 3.13 — not 3.14 (see docs/decisions.md ADR-009)
# Versions are floors verified against PyPI as of Aug 2026; pin exactly
# with `pip freeze > requirements.lock` once the build is working.

langchain>=1.3.14,<2.0
langgraph>=1.2.10,<2.0
langsmith>=0.10.15,<1.0
deepagents>=0.7.1,<1.0

# Model provider — OpenAI (see docs/BUILD_PLAN.md "Budget")
langchain-openai>=1.4.1,<2.0
# langchain-anthropic>=1.5.3,<2.0  # uncomment only if switching providers

# Studio / local dev server
langgraph-cli[inmem]>=0.4.31,<1.0

python-dotenv>=1.0.0

# Dev
pytest>=8.0.0
